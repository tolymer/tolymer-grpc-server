default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: tolymer

test:
  <<: *default
  database: tolymer_test

production:
  url: <%= ENV['DATABASE_URL'] %>
