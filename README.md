# tolymer-grpc-server

## development

Setup and start server:

```
$ bin/setup
$ docker-compose up
```

Reload application:

```
$ docker-compose restart app
```

Run tests:

```
$ docker-compose exec app bin/rspec
```
